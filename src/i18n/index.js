import Link from './Link'
import withIntl from './withIntl'
import { localeData, languages, defaultLocale, ignorePaths } from './config'

export { Link, withIntl, localeData, languages, defaultLocale, ignorePaths }
